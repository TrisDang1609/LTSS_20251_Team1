#pragma once

#include <string>
#include <vector>

namespace params
{
    // OPENCL parameters
    const std::string_view OPENCL_KERNEL_SOURCE = "src/opencl_kernels"; // overall path to OpenCL kernel sources

}